import type { FC } from "react";

const Canvas: FC = () => {
  return (
    <div className="fixed top-20 left-12 p-1 w-[calc(100%-3rem)] h-full">
      <h1>Canvas</h1>
    </div>
  );
};

export default Canvas;
